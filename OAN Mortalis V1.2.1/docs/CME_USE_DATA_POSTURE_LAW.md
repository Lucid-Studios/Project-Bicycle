# CME_USE_DATA_POSTURE_LAW

## Purpose

This note defines the first persisted rest-state operational footing produced
by the install-agreement corridor in `V1.2.1`.

It exists so install assent can yield one bounded `CME`-use posture without
pretending runtime life, service authorization, or later access branching has
already arrived.

## Governing Compression

The current `CME`-use posture remains below runtime.

It records lawful use footing.
It does not activate anything.

## Current Attachment Profile

The only attachment profile admitted in this phase is:

- `research-attached-default`

No paid or less-attached access structure is admitted yet.

## Core Record Standing

`CoreCmeUsePostureRecord` must preserve:

- `research-attached-default`
- `CME` lab-product standing
- `CME` test-status standing
- data-practice posture
- language dataset id
- active language
- locale and jurisdiction
- agreement lineage refs

The current data-practice read is:

- agency-plus-research-context

For Sanctuary pre-governing standing, this posture is still not research
consent. Install may record research-attached footing, but legal-admin
research participation remains separate, explicit, bounded, and revocable
until staged through Lab assets and regional counsel review.

Use this note together with:

- `CRADLETEK_SITE_BINDING_PROFILE_LAW.md`
- `FINAL_CME_DISCLOSURE_AGREEMENT_LAW.md`
- `CME_IP_INHERITANCE_AND_CREATION_SCOPE_LAW.md`

`CoreCmeUsePostureRecord` may support later site-bound pre-Cradle standing.
It does not replace site profile, disclosure assent, or content authority.

In the current phase, it may also support the later `Sanctuary.GEL`
formation data pool as bounded install-side use footing.

It may also support Sanctuary pre-governing standing as a coded footing
record, while remaining below governing `CME`, domain authority,
pre-certification, `RTME`, and runtime authority.

The `GOVERNING_PRIME_CRYPTIC_TEMPLATE_STRUCTURE_BOUNDARY.md` note may later
shape future governed user-data predicate classes from paired Prime/Cryptic
template posture.
Those predicate classes do not collect user data, create consent, admit
research use, profile users, train models, or change the current
`CoreCmeUsePostureRecord`.

## Boundary Rule

`CoreCmeUsePostureRecord` is the first persisted rest-state operational footing
below runtime.

It is not:

- service authorization
- live `RTME` binding
- Atlas mutation
- site-bound pre-Cradle authorization
- `Sanctuary.MoS` standing

## Explicit Non-Grants

This note does not authorize:

- runtime activation
- access-profile switching
- service authorization
- identity minting by posture alone
- later paid branch logic

## Working Summary

`CoreCmeUsePostureRecord` now carries the first lawful `CME`-use footing for
install assent.

Its present profile is exactly `research-attached-default`, and it remains a
rest-state record rather than a live-service act.

Future Governing.Prime/Cryptic user-data predicate structure remains template
posture only until later consent, disclosure, retention, and startup boundaries
separately admit collection.

The `LAB_MIXED_DATA_PRE_AWAKENING_PROOF_RUN_BOUNDARY.md` note may represent a
mixed Lab data manifest as metadata-only proof posture.
It does not change `CoreCmeUsePostureRecord`, create research consent, collect
user data, expose raw content, train models, admit provider visibility, or
grant runtime authority.

The `LAB_DATA_INVENTORY_SCHEMA_BOUNDARY.md` note may classify documented data
as governed metadata-only inventory posture after proof posture and before any
later ingestion boundary.
That inventory does not change `CoreCmeUsePostureRecord`, create consent,
authorize research use, train models, create provider visibility, export model
context, activate runtime, or admit `RTME` movement.

The `LAB_DATA_INVENTORY_EVALUATION_POSTURE_BOUNDARY.md` note may read that
inventory posture for completeness, consistency, scope, and refusal conditions
only.
Evaluation does not change `CoreCmeUsePostureRecord`, make inventoried data
ingestible, authorize use, approve research, collect consent, train models,
activate runtime, or admit `RTME` movement.

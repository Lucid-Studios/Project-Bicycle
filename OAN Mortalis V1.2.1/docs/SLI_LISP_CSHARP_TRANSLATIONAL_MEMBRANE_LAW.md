# SLI_LISP_CSHARP_TRANSLATIONAL_MEMBRANE_LAW

## Purpose

This note defines the line-local membrane between symbolic `SLI.Lisp` emission
and the first bounded C# runtime contract surfaces being carried into
`V1.2.1`.

Its job in this line is narrower than full runtime projection.

It exists to:

- define the first passive symbolic passport family
- define the first passive validation and membrane-decision surfaces
- keep the install-first shell lawful while source carry forward begins

This is the first membrane-facing source batch for `V1.2.1`.

## Governing Compression

`C#` is the instrument body that bounds and projects action.
`Lisp` is the threaded symbolic tensioning governed by `IUTT` lawful
intervals.
`EC` is the acting functor that plays across those prepared relations,
producing live cognition within the admissible resonant field.
The membrane is the bridge geometry that governs how symbolic tension lawfully
becomes embodied runtime obligation.

Two prohibitions govern the whole note:

> No symbolic product may bypass the membrane.

> No runtime surface may self-authorize symbolic promotion.

## Governing Read

Use this note with:

- `ROOTATLAS_REMOTE_SOURCE_BOUNDARY.md`
- `SANCTUARY_GEL_BOOTSTRAP_LAW.md`
- `SANCTUARY_LISP_RTME_SERVICE_LAW.md`
- `SANCTUARY_GOA_CRADLETEK_GOA_AND_CGOA_SPAWN_LAW.md`
- `SOULFRAME_CGOA_CRYPTIC_SEAL_LAW.md`
- `PRODUCTION_FILE_AND_FOLDER_TOPOLOGY.md`

## Install-First Boundary

`V1.2.1` is not yet claiming the full live membrane corridor.

What is admitted now is:

- line-local membrane doctrine
- a first passive `San.Common` passport family
- a passive validator
- a passive membrane decision surface
- a first hosted `SLI.Lisp` bundle body beneath `SanctuaryID.RTME` service law
- a first passive `SLI.Runtime` preserved-field query and recomposition family
- a first passive `SLI.Runtime` candidate-evaluation family beneath that
  preserved-field layer
- a first descriptive carriage schema for already-admitted seam nouns beneath
  the non-executive shell and witnessed trace path

What remains withheld is:

- live adapter corridors
- observational organism read models
- emitted runtime lane sinks
- any claim that membrane ingress is already driving a full working line

## First Engine-To-Hosted-Bundle Seam-Definition Batch

The next doctrine-only seam-definition batch now carried in this line lives in:

- `SLI_ENGINE_LISP_BINDING_CONTRACT.md`
- `OPERATOR_INGRESS_PRECONDITIONS.md`
- `SEAM_REFUSAL_AND_RETURN_LAW.md`

This batch is still bounded.

It gives the line:

- a named binding contract for `SLI.Engine -> SLI.Lisp -> SanctuaryID.RTME`
- an object-level ingress check note beneath the withheld operator edge
- a refusal and return note that keeps seam failure observable

The current line does not yet materialize a separate
`San.Common.MembraneDecisionResult`.
Until that changes lawfully, membrane-decision standing is preserved through
`San.Common.MembraneDecision` on `SLI.Engine.PredicateLandingRequest`.

It does not yet claim:

- live engine-to-hosted-bundle binding
- `RTME` admission shell behavior
- cross-language binding realization

## First `SanctuaryID.RTME` Shell And Witnessed Trace

The next doctrine-only shell-and-trace batch now carried in this line lives in:

- `SANCTUARYID_RTME_SKELETON.md`
- `FIRST_WORKING_MODEL_TRACE_PATH.md`

This batch is still bounded.

It gives the line:

- a named `SanctuaryID.RTME` admission shell
- one witnessed path from candidate evaluation through seam approach into
  hosted receipt
- one witnessed refusal branch that returns as receipt rather than silent drop

It does not yet claim:

- live `RTME` wake
- live bundle execution
- live engine-to-hosted-bundle binding
- cross-language binding realization

## First Descriptive Lisp/C# Carriage Schema

The next doctrine-only carriage note now carried in this line lives in:

- `LISP_CSHARP_BINDING_SCHEMA.md`

This batch is still bounded.

It gives the line:

- a descriptive carrier mapping for already-admitted seam nouns only
- preservation rules for identity-bearing and receipt-bearing fields
- refusal and withhold visibility across Lisp and C# carriage
- explicit non-grants against live binding, realization, and host inference

Its governing sentence is:

> This schema governs carriage, not consequence.

The current line still does not materialize a separate
`San.Common.MembraneDecisionResult`.
The carriage schema therefore preserves seam standing through the actually
carried `San.Common.MembraneDecision` surface.

It does not yet claim:

- live engine-to-hosted-bundle binding
- live execution
- runtime permission by translation

## First Carried Contract Family

The first carried membrane-facing source family now lives in:

- `src/San/San.Common/San.Common.csproj`
- `src/San/San.Common/SymbolicEnvelopeContracts.cs`
- `src/San/San.Common/SymbolicEnvelopeValidation.cs`
- `src/San/San.Common/MembraneDecisionPolicy.cs`

This batch is intentionally passive.

It gives the line:

- `SymbolicEnvelope`
- `SymbolicProductClass`
- `AdmissibilityStatus`
- `ContradictionState`
- `MaterializationEligibility`
- `PersistenceEligibility`
- `MembraneDecision`

plus the first bounded validator and decision policy.

## First Hosted Symbolic Bundle Body

The first hosted symbolic sibling body now lives in:

- `src/SLI/SLI.Lisp/SLI.Lisp.csproj`
- `src/SLI/SLI.Lisp/CrypticLispBundleContracts.cs`
- `src/SLI/SLI.Lisp/GovernedCrypticLispBundleService.cs`
- `src/SLI/SLI.Lisp/LispModuleCatalog.cs`
- `src/SLI/SLI.Lisp/core.lisp`
- `src/SLI/SLI.Lisp/parser.lisp`
- `src/SLI/SLI.Lisp/transport.lisp`
- `src/SLI/SLI.Lisp/witness.lisp`
- `src/SLI/SLI.Lisp/admissibility.lisp`
- `src/SLI/SLI.Lisp/compass.lisp`

This batch is still bounded.

It gives the line:

- a resident hosted module catalog
- a canonical first floor-set for engine-facing symbolic support
- a bundle receipt surface for hosted symbolic residency beneath
  `SanctuaryID.RTME`

It does not yet claim:

- full issued `RTME` wake
- live `EC` binding
- downstream runtime authority beyond membrane-decided passage

## First Preserved-Field Query And Recomposition Family

The first passive preserved-field family now carried in this line lives in:

- `src/SLI/SLI.Runtime/FieldQueryContracts.cs`
- `src/SLI/SLI.Runtime/FieldQueryPolicy.cs`
- `src/SLI/SLI.Runtime/QueryTensionSummary.cs`
- `src/SLI/SLI.Runtime/RecompositionCandidateContracts.cs`

This batch is still bounded.

It gives the line:

- typed field query axes and results
- explicit tension summaries that do not normalize contradiction away
- provenance-bearing recomposition candidates
- a bounded `IFieldQueryEngine` that keeps query non-sovereign

It does not yet claim:

- live field mutation
- autonomous recomposition landing
- operator realization beyond passive retrieval review

## First Passive Candidate-Evaluation Family

The first passive candidate-evaluation family now carried in this line lives
in:

- `src/SLI/SLI.Runtime/RecompositionCandidateEvaluationContracts.cs`
- `src/SLI/SLI.Runtime/RecompositionCandidateEvaluationPolicy.cs`

This batch is still bounded.

It gives the line:

- typed burden axes and results for candidate review
- typed recovery classes and candidate-only outcomes
- a bounded evaluation policy that may retain, cleave, defer, or reject
  recomposition candidates without realizing them

It does not yet claim:

- operator realization
- child issuance from cleave posture
- accepted engram or proto-engram standing

## Withheld Operator-Realization Seam

The next operator-seam doctrine now carried in this line lives in:

- `OPERATOR_REALIZATION_BOUNDARY_LAW.md`
- `OPERATOR_REALIZATION_WITNESS_AND_PROHIBITION_LAW.md`
- `OPERATOR_REALIZATION_ADMISSION_SEQUENCE.md`

This batch remains doctrine-only and withheld.

It gives the line:

- the constitutional edge of later operator realization
- the required witness floor and prohibitions for later seam ingress
- the exact admission order from passive candidate preparation into the
  withheld seam

It does not yet claim:

- zero-behavior stubs
- live operator ingress
- live `E`, `A`, `Lambda`, or `M` execution

## Boundary Statement

The line should now read this seam in four offices.

### `SLI.Lisp`

`SLI.Lisp` is the symbolic metabolism layer.
It may emit symbolic products under lawful interval space.
It may not by itself:

- materialize runtime obligation
- mutate canon
- promote itself into continuity-bearing standing

### Membrane

The membrane validates, classifies, passports, and decides symbolic products
before they may become bounded runtime obligation.

### `San.Common`

`San.Common` currently carries the first passive C# membrane-facing contract
surface for the new line.

### Downstream Runtime

Later runtime families may only act on membrane products that are:

- typed
- passported
- validated
- membrane-decided

and still within the authority ceiling granted by that decision.

## Symbolic Product Classes

`product_class` is distinct from `family`.

- `family` says what kind of symbolic thing the product is
- `product_class` says how the membrane may treat it
- `intent` is the family-local requested use inside that class

The first carried class lattice is:

- `ReadProduct`
- `CandidateProduct`
- `DirectiveProduct`
- `CollapseProduct`

This keeps three things from collapsing:

- observation into action
- action into persistence
- persistence into canon

## Passport Minimum

Every symbolic product crossing the membrane should carry:

- `origin`
- `family`
- `product_class`
- `intent`
- `admissibility`
- `contradiction_state`
- `materialization_eligibility`
- `persistence_eligibility`
- `trace_id`

These are not optional hints.
They are the minimum constitutional passport.

## Decision Lattice

The first carried membrane decision set remains:

- `accept`
- `transform`
- `defer`
- `refuse`
- `collapse`

If a proposed action cannot be truthfully named by one of these, it should
remain outside this batch.

## Hard Prohibitions

1. No direct `SLI.Lisp -> persistence`.
2. No direct `SLI.Lisp -> canon`.
3. No runtime surface may infer missing passport fields.
4. No symbolic product may upgrade its own admissibility.
5. No downstream runtime surface may treat coherent output as executable
   authority merely because it is coherent.

## Implementation Consequences

This note now governs the first `V1.2.1` source carry-forward batch.

That first membrane batch now hangs under the first line-local solution graph
at `San.sln`.

The next carry-forward order after this crossing has now reached:

1. first `SLI.Engine` sibling project
2. first hosted `SLI.Lisp` sibling project
3. first passive `SLI.Runtime` governance project
4. first passive field-query and recomposition family
5. first passive candidate-evaluation family
6. doctrine-only seam-definition batch for
   `SLI.Engine -> SLI.Lisp -> SanctuaryID.RTME`
7. doctrine-only `SanctuaryID.RTME` admission shell
8. first witnessed first-working-model trace path
9. withheld operator-realization seam
10. doctrine-only cross-language binding schema for already-admitted seam nouns
11. later zero-behavior operator seam stubs only if naming discipline requires them
12. later operator realization beneath preserved-field, candidate-evaluation, and withheld-operator-seam law
13. later active runtime binding

This keeps the install-first shell truthful while real source materialization
begins.
